const mathFunctions = require("./functions");
// window refers to the browser - node.js does not have a browser to reference
// console.log(window);

console.log("home");

console.log(mathFunctions.quadraticFormula(1, 2, 1));
